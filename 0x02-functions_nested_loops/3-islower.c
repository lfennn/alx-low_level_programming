#include "main.h"

/**
 * _islower - checks if a character is lowercase.
 * @c: character to check.
 * Return: 0 if lowercase, 1 if not.
 */
int _islower(int cif (c >= 'a' && c <= 'z')
{
return 1;
}
else
{
return 0;
}
}
